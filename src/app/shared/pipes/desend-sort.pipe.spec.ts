import { DesendSortPipe } from './desend-sort.pipe';

describe('DesendSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DesendSortPipe();
    expect(pipe).toBeTruthy();
  });
});
